package Game;

/**
 * Interface that is used in every class that needs to reset something for a new game to start.
 */
public interface Resettable {
     /**
      * Resets stuff.
      */
     void reset();
}
